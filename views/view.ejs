<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Shoe Details</title>
    <link rel="stylesheet" href="/path/to/your/styles.css" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@docsearch/css@3"
    />

    <style>
      /* Add your custom styles here */
      body {
        font-family: Arial, sans-serif;
        background-color: #343a40; /* Dark background color */
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 800px;
        margin: 20px auto;
        padding: 20px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        border-radius: 5px;
        display: flex;
        flex-direction: row;
      }
      .header {
        background-color: #333;
        color: #fff;
        padding: 10px 20px;
        text-align: center;
      }
      .shoe-image {
        width: 35%; /* Adjust left side width */
        padding-right: 20px; /* Add some space between image and details */
      }
      .shoe-image img {
        max-width: 100%;
        height: auto;
        border-radius: 5px;
        box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
      }
      .shoe-details {
        width: 50%; /* Adjust right side width */
        padding: 20px;
        border-radius: 5px;
      }
      .buttons {
        margin-top: 20px;
        display: flex;
        justify-content: space-between;
      }
      .btn {
        background-color: green;
        color: #fff;
        border: none;
        border-radius: 5px;
        padding: 10px 20px;
        cursor: pointer;
        transition: background-color 0.3s ease;
      }
      .btn:hover {
        background-color: #2e7d32; /* Darken the background color on hover */
      }
      .bid-amount {
        margin-bottom: 20px;
      }
    </style>
  </head>
  <body data-bs-theme="dark">
    <!-- Header -->
    <header data-bs-theme="dark">
      <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <%- include('header') %>
      </nav>
    </header>

    <div
      class="container"
      style="margin-top: 100px; background-color: dark"
      data-bs-theme="dark"
    >
      <!-- Display shoe image on the left side -->
      <div class="shoe-image" style="height: 600px">
        <img
          src="data:<%= shoe.Pic.contentType %>;base64,<%= shoe.Pic.data.toString('base64') %>"
          alt="<%= shoe.name %>"
          class="product-image"
        />
      </div>

      <!-- Display shoe details on the right side -->
      <div class="shoe-details" style="margin-top: 50px">
        <h2><%= shoe.name %></h2>
        <p>Size: <%= shoe.size %></p>
        <p>Price: $<%= shoe.price %></p>
        <p>Description: <%= shoe.description %></p>
        <p>Material: <%= shoe.materialUsed %></p>
        <p>Category: <%= shoe.category %></p>
        <p>Buying Date: <%= shoe.buyingDate %></p>
        <p>ownerName: <%= shoe.ownerName %></p>
        <p>ownerEmail: <%= shoe.ownerEmail %></p>
        <form action="/bid/<%= shoe._id %>" method="POST">
          <div class="bid-amount">
            <label for="bidAmount">Bid Amount:</label>
            <input
              type="number"
              id="bidAmount"
              name="bidAmount"
              class="form-control"
              placeholder="Enter your bid amount"
              required
            />
            <input type="hidden" name="shoeId" value="<%= shoe._id %>" />
          </div>
          <div class="buttons">
            <button
              type="submit"
              class="btn"
              style="background-color: green; width: 150px; margin-left: 0px"
            >
              Bid
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
